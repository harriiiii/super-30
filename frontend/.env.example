# No secrets required for the frontend.
# In development, Vite proxies /api requests to http://localhost:3000 (the backend).
# In production, the backend serves the built frontend from ../frontend/dist.
